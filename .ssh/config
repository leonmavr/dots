# Instructions:
# -------------
# To clone from a configured host, e.g.:
#
# Host github.com-hostname
#   HostName github.com
#   User leonmavr
#   IdentityFile ~/.ssh/id_ed25519_hostname
# 
# You need to paste ~/.ssh/id_ed25519_hostname.pub in the hosts's
# ssh keys and then clone as follows:
# git clone git@github.com-hostname:github_username/repo-url.git

Host aur.archlinux.org                   
  IdentityFile ~/.ssh/aur
  User leonmavr 
