########## Instructions ##########
# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

# If using Ctrl/ Fn, Ctrl and Fn and swapped in my laptop
set $mod Mod4 
set $alt Mod1

# Font for i3blocks etc. 
font pango:DejaVu Sans Mono, Font Awesome 11 

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill
bindsym $mod+q kill

# The (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that installed.
#bindsym $mod+d exec --no-startup-id i3-dmenu-desktop
bindsym $mod+d exec rofi -modi "window#run#ssh#drun" -show drun -columns 2 -lines 10 -font "Oxanium 13"

########## Startup ##########
exec_always --no-startup-id compton -b --config ~/.config/compton/config_blur > /dev/null 2>&1 
exec --no-startup-id mpd
exec_always --no-startup-id setxkbmap -layout gb,gr
exec_always --no-startup-id setxkbmap -option 'grp:alt_shift_toggle'
#exec --no-startup-id killall polybar > /dev/null 2>&1 
#exec --no-startup-id nohup polybar top > /dev/null 2>&1 
# temporary - fix it
#exec_always --no-startup-id $HOME/.config/polybar/startup.sh
exec_always --no-startup-id synclient TapButton1=1
exec_always systemctl start cronie 
exec_always --no-startup-id ~/.config/polybar/startup.sh
exec_always --no-startup-id conky -c ~/.config/conky/conkylua
exec_always --no-startup-id nm-applet 


# change focus
# TODO: use another key
#bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h  move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# adjust gaps
bindsym $mod+g gaps outer all minus 5; gaps inner all minus 5
bindsym $mod+Shift+g gaps outer all plus 5; gaps inner all plus 5

# change container layout (stacked, tabbed, toggle split)
#bindsym $mod+s layout [stacking or tabbed or split]

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

for_window [class="Yad"] floating enable"
# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

########## Jump between workspaces ##########
# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# Switch between workspaces
bindsym $alt+Tab workspace back_and_forth
bindsym $mod+Tab workspace next
bindsym Shift+$mod+Tab workspace previous 
workspace_auto_back_and_forth yes


########## Reload/ restart/ exit ##########
# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"
# B -> bar
bindsym $mod+Shift+b exec ~/.config/polybar/startup.sh


########## Modes ##########
# Press Alt+another key to enter a mode.
# Esc/ Return to leave.
#
######
bindsym $alt+g mode "gaps"
mode "gaps" {
	# Can also choose "current" instead of "outer"
	bindsym o gaps outer all minus 5
	bindsym Shift+o gaps outer all plus 5
	bindsym i gaps inner all minus 5
	bindsym Shift+i gaps inner all plus 5
    # back to normal: Enter or Escape or $mod+r
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}

bindsym $alt+r mode "resize"
# resize window (you can also use the mouse for that)
mode "resize" {
    # These bindings trigger as soon as you enter the resize mode

    # Pressing left will shrink the window’s width.
    # Pressing right will grow the window’s width.
    # Pressing up will shrink the window’s height.
    # Pressing down will grow the window’s height.
    bindsym h resize grow width 5 px or 5 ppt
    bindsym j resize grow height 5 px or 5 ppt
    bindsym k resize shrink height 5 px or 5 ppt
    bindsym l resize shrink width 5 px or 5 ppt
    bindsym H resize grow width 15 px or 15 ppt
    bindsym J resize grow height 15 px or 15 ppt
    bindsym K resize shrink height 15 px or 15 ppt
    bindsym L resize shrink width 15 px or 15 ppt

    # same bindings, but for the arrow keys
    bindsym Left resize shrink width 5 px or 5 ppt
    bindsym Down resize grow height 5 px or 5 ppt
    bindsym Up resize shrink height 5 px or 5 ppt
    bindsym Right resize grow width 5 px or 5 ppt
    bindsym Shift+Left resize grow width 15 px or 15 ppt
    bindsym Shift+Down resize grow height 15 px or 15 ppt
    bindsym Shift+Up resize shrink height 15 px or 15 ppt
    bindsym Shift+Right resize shrink width 15 px or 15 ppt

    # back to normal: Enter or Escape or $mod+r
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}


######
bindsym $alt+t mode "terminals"
mode "terminals" {
	bindsym 1 exec i3-sensible-terminal
	bindsym 2 exec i3-sensible-terminal; exec i3-sensible-terminal
	# TODO: order doesn't work well
	bindsym 3 exec i3-sensible-terminal; exec i3-sensible-terminal;split h; exec i3-sensible-terminal
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"

}

bindsym $alt+a mode "apps"
mode "apps" {
	bindsym f exec firefox 
	bindsym s exec skypeforlinux  
	bindsym t exec i3-sensible-terminal 
	bindsym Shift+t exec thunar 
	bindsym g exec gedit 
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}

########## Send apps to specific workspaces ##########
# do `xprop` to find out class name
assign [class="firefox*"] $ws2
assign [class="Thunar"] $ws3
assign [class="Skype"] $ws4
for_window [title="^Spotify*"] move to workspace $ws3

########## Window style ##########
# Gaps around windows
gaps inner 10
gaps outer 0
# smart gaps on

# No borders
new_window none 
new_float none
#border_radius 30

########## Hotkeys ##########
# Run `xev` to find out the hardware name of each key
# Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5% #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound

# Sreen brightness controls - use `light` from AUR
bindsym XF86MonBrightnessUp exec sudo light -A 10 # increase screen brightness
bindsym XF86MonBrightnessDown exec sudo light -U 10 # decrease screen brightnes

# Media player controls
bindsym XF86AudioPlay exec mpc toggle 
#bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec mpc next
bindsym XF86AudioPrev exec mpc prev 

# XF86WebCam = Fn+F6 on my Thinkpad
# Take screenshot of the whole screen and save to clipboard
bindsym XF86WebCam exec scrot /tmp/screenshot.png && xclip -selection clipboard -t image/png -i /tmp/screenshot.png
# Take screenshot of an area and save to clipboard
bindsym $Mod+XF86WebCam exec import /tmp/screenshot.png && xclip -selection clipboard -t image/png -i /tmp/screenshot.png
# Fn+F3 => lock 
bindsym XF86ScreenSaver exec ~/.config/i3lock-color/lock.sh
# F2 => toggle touchpad - useful when I write
bindsym F2 exec ~/.config/misc/toggle_touchpad.sh

###############
